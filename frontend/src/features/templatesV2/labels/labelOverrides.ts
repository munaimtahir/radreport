/**
 * labelOverrides.ts
 * 
 * Manual overrides for labelResolver where deterministic logic fails.
 */

export const labelOverrides: Record<string, string> = {
    // Example:
    // 'kid_r_length_cm': 'Right Kidney Length (cm)',
};
