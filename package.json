{
  "name": "radreport",
  "version": "1.0.0",
  "description": "This repo is a **merge-friendly, independent RIMS** designed to later merge into your LIMS with minimal pain. It matches the same architectural philosophy: Django + DRF + Postgres + JWT + (optional) Celery/Redis + React/Vite.",
  "main": "index.js",
  "directories": {
    "doc": "docs",
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "e2e": "playwright test",
    "e2e:smoke": "playwright test e2e/tests/smoke.spec.ts",
    "e2e:ui": "playwright test --ui",
    "e2e:report": "playwright show-report e2e/artifacts/playwright-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.58.1",
    "dotenv": "^17.2.4",
    "typescript": "^5.9.3"
  }
}